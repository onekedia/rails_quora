<%= form_with(model: question_topic, local: true) do |form| %>
  <% if question_topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(question_topic.errors.count, "error") %> prohibited this question_topic from being saved:</h2>

      <ul>
      <% question_topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :question_id %>
    <%= form.number_field :question_id %>
  </div>

  <div class="field">
    <%= form.label :topic_id %>
    <%= form.number_field :topic_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
